import {ArrowRight,BedDouble,Maximize,Car,Bath} from 'lucide-react';import {Property,priceText} from '@/data/properties';import {Locale,regions,route,tr,words} from '@/data/site';
export function PropertyCard({p,l}:{p:Property;l:Locale}){return <a className="property-card" href={route(l,'imoveis/'+p.slug)}><div className="card-image"><img src={p.coverImage} srcSet={`${p.coverImage.replace(".webp","-small.webp")} 800w, ${p.coverImage} 1600w`} sizes="(max-width:760px) 100vw, 46vw" alt={tr(l,'Imagem ilustrativa de ','Illustrative image of ')+p.title[l]} loading="lazy" width="800" height="600"/><span className="property-tag">{words[l][p.purpose]}</span><span className="card-go"><ArrowRight size={21}/></span></div><div className="card-location">{regions.find(r=>r.slug===p.region)?.name} <span>· BÚZIOS</span></div><h3>{p.title[l]}</h3><div className="specs">{p.bedrooms&&<span><BedDouble/>{p.bedrooms} {tr(l,'quartos','beds')}</span>}{p.bathrooms&&<span><Bath/>{p.bathrooms}</span>}{p.parkingSpaces&&<span><Car/>{p.parkingSpaces}</span>}<span><Maximize/>{p.builtArea||p.landArea} m²</span></div><div className="card-price">{priceText(p,l)}<span>{tr(l,'Exemplo','Demo')}</span></div></a>}
export function PropertyGrid({items,l}:{items:Property[];l:Locale}){return <div className="property-grid">{items.map(p=><PropertyCard key={p.id} p={p} l={l}/>)}</div>}
